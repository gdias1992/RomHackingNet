// Health check
export { useHealth } from "./useHealth";

// Metadata hooks
export {
  useConsoles,
  useGenres,
  useHackCategories,
  useLanguages,
  useMetadata,
  usePatchStatuses,
} from "./useMetadata";

// Game hooks
export { useGame, useGameHacks, useGameTranslations, useGames } from "./useGames";

// Hack hooks
export { useHack, useHackImages, useHacks } from "./useHacks";

// Translation hooks
export {
  useTranslation,
  useTranslationImages,
  useTranslations,
} from "./useTranslations";

// Utility hooks
export { useUtilities, useUtility } from "./useUtilities";

// Document hooks
export { useDocuments, useDocument } from "./useDocuments";

// Homebrew hooks
export { useHomebrews, useHomebrew } from "./useHomebrew";

// Common utility hooks
export { useDebounce } from "./useDebounce";
export { useUrlState } from "./useUrlState";
